service: tidy-contactform

useDotenv: true

provider:
  name: aws
  region: eu-west-2
  runtime: nodejs16.x
  deploymentMethod: direct

functions:
  tidy-contactform:
    handler: index.handler
    name: tidy-contactform
    events:
      - httpApi:
          method: POST
          path: /
