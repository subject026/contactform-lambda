{
  "name": "contactform-lambda",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "invoke:local": "serverless invoke local --function contactform-lambda",
    "deploy": "yarn build && serverless deploy",
    "update": "yarn build && serverless deploy function --function contactform-lambda"
  },
  "devDependencies": {
    "serverless": "^3.22.0",
    "typescript": "^4.8.3"
  },
  "dependencies": {
    "zod": "^3.19.1"
  }
}
